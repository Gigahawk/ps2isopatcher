[project]
name = "ps2isopatcher"
version = "0.1.0"
description = "Modify PS2 ISO files"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "click>=8.1.8",
]

[project.scripts]
ps2isopatcher = 'ps2isopatcher.cli:cli'
